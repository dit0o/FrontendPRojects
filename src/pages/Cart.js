import React from "react";

export const Cart = () => {
    return (
        <section className="cart1">
            <div className="cart2">
                <h2 className="cart">Your cart is empty</h2>
            </div>
        </section>
    );
};
